<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atixiansoftware.samadhan">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".User.ProcedureActivity"
            android:theme="@style/Nobar" />
        <activity
            android:name=".User.VendorActivity"
            android:theme="@style/EditorTheme" />
        <activity android:name=".User.OutdoorProject"/>
        <activity android:name=".User.IndoorProject"/>
        <activity android:name=".User.ProjectActivity"/>
        <activity android:name=".User.ChoiseActivity"/>

        <activity android:name=".User.Manufacture"
            android:theme="@style/LoginTheme" />

        <activity android:name=".User.SplashActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".User.HomeActivity"
            android:theme="@style/LoginTheme" />
        <activity android:name=".User.LoginActivity"
            android:theme="@style/Nobar">
        </activity>
    </application>

</manifest>